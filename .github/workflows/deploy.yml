# このファイルは新しい運用方式では使用されません
# 個人の gh-pages ブランチでの手動運用に変更されました
# 
# 以下のコードは参考として残してありますが、
# 現在の運用では実行されません

# name: Build and Deploy to GitHub Pages
# 
# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:
# 
# permissions:
#   contents: write
# 
# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#       
#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'
#           cache: 'npm'
#       
#       - name: Install dependencies
#         run: npm ci
#       
#       - name: Build
#         run: npm run build
#       
#       - name: Deploy to gh-pages
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./dist
#           publish_branch: gh-pages
